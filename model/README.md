# TensorFlow.js model files

Place your converted TensorFlow.js model artifacts in this folder:

- `model.json`
- one or more `group*-shard*.bin` weight files

The app will automatically load `model/model.json` at startup.
